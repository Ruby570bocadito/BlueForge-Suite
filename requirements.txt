# =============================================================================
# Bluesky - Dependencias Python
# =============================================================================
# Instalación: pip install -r requirements.txt
#
# Instalación completa (BLE + Web):
#   pip install -r requirements.txt flask bleak pybluez scapy cryptography
#
# O usando extras (desde pyproject.toml):
#   pip install bluesky[all]
#   pip install bluesky[web]     # Solo web dashboard
#   pip install bluesky[ble]     # Solo Bluetooth LE
#   pip install bluesky[dev]     # Desarrollo

# ─── Core ──────────────────────────────────────────────────────────────────
click>=8.0.0
colorama>=0.4

# ─── CLI interactiva ──────────────────────────────────────────────────────
rich>=10.0.0

# ─── Comunicación serie ────────────────────────────────────────────────────
pyserial>=3.5

# ─── Testing ──────────────────────────────────────────────────────────────
pytest>=7.0
pytest-cov>=4.0

# ─── Web Dashboard ──────────────────────────────────────────────────────────
flask>=3.0.0

# ─── Bluetooth (obligatorio para funcionalidad real) ────────────────────────
bleak>=0.14.0          # BLE cross-platform
scapy>=2.4.5           # Packet crafting (KNOB, BLUFFS, BTLEJack, SweynTooth)
cryptography>=3.4.0    # Criptografía BLE (Crackle, cifrados)

# ─── Herramientas adicionales ───────────────────────────────────────────────
requests>=2.25.0

# ─── Opcionales (instalar si es necesario) ─────────────────────────────────
# pybluez>=0.22        # Bluetooth Classic (solo Linux legacy, no recomendado)
# pycryptodome>=3.0    # Alternativa cryptography (AES, ECC)
# bs4>=4.10.0          # BeautifulSoup - parsing HTML (OSINT)
# lxml>=4.9.0          # XML parsing

# ─── Notas de compatibilidad ──────────────────────────────────────────────
# Windows: Usar bleak para BLE. PyBluez no funciona en Windows moderno.
# Linux:   Usar BlueZ tools (bluetoothctl, hciconfig). Scapy para paquetes.
# Termux:  Usar Termux:API (termux-bluetooth-*). Fallback a BlueZ tools.
# WSL:     No tiene acceso a hardware Bluetooth. Usar modo --dry-run.
